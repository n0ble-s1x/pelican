[advisories]
# RUSTSEC-2024-0436 — paste, unmaintained, transitive via eframe → wgpu →
# metal → paste. Not a vulnerability; no exploit path. Mirrored in deny.toml.
# Revisit when upstream wgpu replaces paste.
ignore = ["RUSTSEC-2024-0436"]
